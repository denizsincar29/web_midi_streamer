[Unit]
Description=WebRTC Signaling Server (jamrtc)
After=network.target
Documentation=https://github.com/denizsincar29/web_jamrtc

[Service]
Type=simple
User=denizsincar29
Group=deniz
WorkingDirectory=/home/deniz/signaler
EnvironmentFile=-/home/deniz/signaler/.env
ExecStart=/home/deniz/signaler/signaler -addr :8987

# Restart on crash, but not on intentional stop
Restart=on-failure
RestartSec=5

# Logging — journalctl -u signaler -f
StandardOutput=journal
StandardError=journal
SyslogIdentifier=signaler

# Security hardening
NoNewPrivileges=yes
PrivateTmp=yes
ProtectSystem=strict
ProtectHome=read-only
ReadWritePaths=/home/deniz/signaler

[Install]
WantedBy=multi-user.target
